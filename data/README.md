# Data

This directory is reserved for local datasets and dataset metadata.

Do not download a dataset yet. Before adding data, agree on the dataset source, domains, train/validation/test splits, face preprocessing, and evaluation protocol so every team member uses the same setup.

Large datasets should not be committed to Git. Keep only lightweight documentation, manifests, or configuration here.
